<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch.xsd">

    <!-- In Memory Transaction Manager -->
    <bean id="transactionManager" class="org.springframework.batch.support.transaction.ResourcelessTransactionManager"/>

    <!-- Job Repository using ResourcelessJobRepository -->
    <bean id="customJobRepository" class="org.springframework.batch.core.repository.support.ResourcelessJobRepository"/>

    <!-- Step 1: Load CSV and map to BeanWrapperFieldSetMapper -->
    <bean id="itemReader" class="org.springframework.batch.item.file.FlatFileItemReader">
        <property name="resource" value="file:${nas.input.path}"/>
        <property name="lineMapper">
            <bean class="org.springframework.batch.item.file.mapping.DefaultLineMapper">
                <property name="lineTokenizer">
                    <bean class="org.springframework.batch.item.file.transform.DelimitedLineTokenizer">
                        <property name="delimiter" value=","/>
                        <property name="names" value="name,age"/>
                    </bean>
                </property>
                <property name="fieldSetMapper">
                    <bean class="org.springframework.batch.item.file.mapping.BeanWrapperFieldSetMapper">
                        <property name="targetType" value="com.unicredit.tap.model.IF03Mapping"/>
                    </bean>
                </property>
            </bean>
        </property>
    </bean>

    <!-- Jaxb2Marshaller Bean for XML Output -->
    <bean id="jaxbMarshaller" class="org.springframework.oxm.jaxb.Jaxb2Marshaller">
        <property name="classesToBeBound">
            <list>
                <value>com.unicredit.tap.model.XmlMapping</value>
            </list>
        </property>
    </bean>

    <!-- StaxEventItemWriter for Writing XML -->
    <bean id="xmlItemWriter" class="org.springframework.batch.item.xml.StaxEventItemWriter">
        <property name="resource" value="file:${nas.output.path}"/>
        <property name="marshaller" ref="jaxbMarshaller"/>
        <property name="rootTagName" value="persons"/>
    </bean>

    <!-- Define Job -->
    <job id="IF03CvsJob" xmlns="http://www.springframework.org/schema/batch" job-repository="customJobRepository">
        <step id="step1">
            <tasklet>
                <chunk reader="itemReader" writer="xmlItemWriter" commit-interval="1000"/>
            </tasklet>
        </step>
    </job>

</beans>
